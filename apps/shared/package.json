{
  "name": "@stableflow/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm typecheck",
    "dev": "tsc -p tsconfig.json",
    "test": "echo \"No tests yet for shared\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
